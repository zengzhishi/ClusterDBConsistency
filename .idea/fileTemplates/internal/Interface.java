#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
#set ($SITE = "http://uestc.xyz")
#set ($GROUP = "lug")
/**
 * ${DESCRIPTION}
 * ${SITE}
 * @author ${USER}
 * @group ${GROUP}
 * @create ${DATE}
 */
public interface ${NAME} {
}
